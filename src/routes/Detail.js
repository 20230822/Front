// npm i styled-reset설치 후 사용
import { Reset } from "styled-reset";

function Detail() {
  return (
    <div className="detail">
      <Reset />

      <h1>detail</h1>
    </div>
  );
}

export default Detail;