// npm i styled-reset설치 후 사용
import { Reset } from "styled-reset";

function Search() {
  return (
    <div className="search">
      <Reset />

      <h1>search</h1>
    </div>
  );
}

export default Search;